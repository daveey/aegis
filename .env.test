# Test Environment Configuration for Aegis
# This file contains test-specific settings separate from production

# ============================================================================
# REQUIRED: Asana Configuration
# ============================================================================
# Get these from your Asana account:
# 1. PAT: https://app.asana.com/0/my-apps
# 2. Workspace GID: Check URL when in workspace
# 3. Test Project: Create a dedicated test project and get GID from URL

ASANA_ACCESS_TOKEN=your_asana_pat_here
ASANA_WORKSPACE_GID=your_workspace_gid
ASANA_TEST_PROJECT_GID=your_test_project_gid

# For orchestrator tests (if using portfolio)
ASANA_PORTFOLIO_GID=your_portfolio_gid_or_test_value

# ============================================================================
# Database Configuration (Test DB - separate from production!)
# ============================================================================
# Use port 5433 to avoid conflicts with production DB on 5432
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/aegis_test

# Redis (use different DB number for tests)
TEST_REDIS_URL=redis://localhost:6379/1

# ============================================================================
# OPTIONAL: Anthropic Configuration
# ============================================================================
# Only needed for live agent tests (most tests use mocks)
# Set RUN_LIVE_TESTS=1 to enable live API tests

ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_MAX_TOKENS=4096

# ============================================================================
# Test Configuration
# ============================================================================

# Enable live API tests (costs money!)
# RUN_LIVE_TESTS=0

# Test execution settings
POLL_INTERVAL_SECONDS=5
MAX_CONCURRENT_TASKS=3

# Logging for tests
LOG_LEVEL=DEBUG
LOG_FORMAT=console

# Feature flags (usually disabled for basic tests)
ENABLE_VECTOR_DB=false
ENABLE_MULTI_AGENT=false

# Vector DB (only if testing vector features)
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=aegis_test
